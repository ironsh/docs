{
  "name": "ironsh-docs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vocs dev --port 4000",
    "build": "vocs build",
    "postbuild": "node scripts/sitemap.mjs",
    "preview": "vocs preview",
    "start": "vocs preview",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vocs": "^1.4.1"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
